# Frontend: leave empty for local Vite proxy; set to the public FastAPI URL in Vercel.
VITE_API_URL=
VITE_WS_URL=

# Backend values belong in the backend host's environment only.
# DATABASE_URL=postgresql+asyncpg://...
# JWT_SECRET=generate-a-long-random-secret
